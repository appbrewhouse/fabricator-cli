#! /bin/bash

export DB_HOSTNAME=${rds_db_hostname}
export DB_PORT=${rds_db_port}
export DB_NAME=${rds_db_name}
export DB_USERNAME=${rds_db_username}
export DB_PASSWORD=${rds_db_password}
export SERVER_PORT=${server_port}

docker run --log-driver=awslogs --log-opt awslogs-group=${awslog_group} -d \
-e DB_HOSTNAME='${rds_db_hostname}' \
-e DB_PORT='${rds_db_port}' \
-e DB_NAME='${rds_db_name}' \
-e DB_USERNAME='${rds_db_username}' \
-e DB_PASSWORD='${rds_db_password}' \
-p 8080:${server_port} appbrewhouse/feteclub-api
